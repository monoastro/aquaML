# 1. Edit config.py — change MODEL_NAME to one of:
#    "DecisionTree", "RandomForest", "KNN"

# 2. Train the model
python train.py

# 3. Run inference with the saved model
python inference.py

# 4. serial 
python serial_monitor.py --mock --interval 1
